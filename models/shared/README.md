# Shared models

Place reusable staging or intermediate models here only when two or more Pentaho
projects depend on the same definition. Domain-specific logic belongs in its
`pentaho_project_N` directory.

